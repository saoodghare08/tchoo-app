import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.example.sns',
  appName: 'HURTEX',
  webDir: 'dist/sou-lnsou-l',
  bundledWebRuntime: false
};

export default config;
